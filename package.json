{
  "name": "re-hooks",
  "version": "0.1.0",
  "description": "A bucket of hooks for ReScript React applications",
  "license": "MIT",
  "scripts": {
    "clean": "bsb -clean-world && rm -fr lib",
    "build": "bsb -make-world",
    "test": "yarn build && node test/Main_Test",
    "example:start": "parcel example/index.html"
  },
  "devDependencies": {
    "bs-platform": "^8.4.2",
    "parcel-bundler": "^1.12.4",
    "react-dom": "^17.0.1"
  },
  "dependencies": {
    "@rescript/react": "^0.10.1",
    "bs-webapi": "^0.19.1",
    "react": "^17.0.1"
  }
}
