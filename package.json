{
  "name": "re-hooks",
  "version": "0.1.0",
  "description": "A bucket of hooks for ReScript React applications",
  "license": "MIT",
  "scripts": {
    "build": "rescript build",
    "clean": "rescript clean && rm -fr lib",
    "format:fix": "rescript format -all",
    "example:start": "parcel example/index.html"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "react-dom": "^17.0.2",
    "rescript": "^9.1.4"
  },
  "dependencies": {
    "@rescript/react": "^0.10.3",
    "bs-webapi": "^0.19.1",
    "react": "^17.0.2"
  }
}
